services:
  - type: web
    name: finpulse-pengatur-keuangan
    env: node
    buildCommand: npm run render-build
    startCommand: npm start
    envVars:
      - key: NODE_VERSION
        value: 20.10.0
